# Docker-Space stack: all pinned to share huggingface-hub <1.0 (no gradio-6.19 conflict).
# torch is installed separately (CPU build) in the Dockerfile.
gradio==4.44.1
sentence-transformers==3.3.1
huggingface-hub==0.25.2
numpy==1.26.4
pandas==2.2.2

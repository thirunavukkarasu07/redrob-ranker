# ---- ranking step (src/rank.py) needs ONLY these (CPU, no network) ----
numpy==2.1.3
pandas==2.3.3
orjson==3.11.8

# ---- offline embedding pre-computation (src/embed.py, src/anchors.py) ----
# Not needed at ranking time; used once to build artifacts/emb.npy + anchor vectors.
# (transformers/tokenizers are intentionally unpinned — sentence-transformers resolves a
#  compatible transformers<5.0.0 itself; pinning them creates a dependency conflict.)
sentence-transformers==5.1.1
torch==2.8.0
